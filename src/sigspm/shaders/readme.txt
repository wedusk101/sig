
To rebuild the built-in shaders:

1) Call genpredefspmshaders.bat from this folder. A new file spm_predef_shaders.inc should be created.

2) Move the new spm_predef_shaders.inc to the parent folder, replacing the previous one.
